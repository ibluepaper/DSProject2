#include "hash_table_class.h"
#include <string>

int stringToInt(string str){
    int len = str.size();
    int sum = 0;
    for (int i = 0; i < len; i++)
      sum += int(str[i]);
    return sum;
}

int main(){
  

}
